import type { BaseLayoutProps } from "fumadocs-ui/layouts/shared";

export const baseOptions: BaseLayoutProps = {
  nav: {
    title: (
      <span className="flex items-center justify-center font-extrabold text-2xl bg-clip-text tracking-tight select-none">
        Soldevkit UI
      </span>
    ),
  },
};
